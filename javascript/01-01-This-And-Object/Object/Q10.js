console.log({a:1}=={a:1})

console.log({a:1}==={a:1})

// False
// False

// Because it will compare refernce of two object , and it doesn't matter if those are strictly compared or not.

// So False. 